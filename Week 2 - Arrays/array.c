#include <stdio.h>

int main (void)
{
    int array[3];
    array[0] = 3;
    array[1] = 1;
    array[2] = 2;
    
    printf("The values of the array are: %i, %i, %i\n", array[0], array[1], array[2]);
}